# Large local data exports — exceed Vercel's 100MB/file limit and are not read
# by the deployed Next.js app (they're raw exports / local analysis inputs).
shopify_orders_l24m.csv
data/lifecycle-customers.json

# Not part of the Next.js runtime bundle.
worker/
docs/
lifecycle_dashboard_mockup.html

# TEMPORARY — in-progress Learning Loop work that does not yet type check
# (DimensionValueAgg has no `rpr` property). Nothing imports it, so excluding it
# from the upload keeps the production build green without editing half-finished
# code. DELETE THIS LINE once the file compiles.
src/lib/performance-memory.ts
