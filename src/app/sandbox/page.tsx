"use client";

// Intentionally empty. This is scratch real estate for testing whether we can
// pull a specific piece of data from a platform. Build the probe here.
export default function SandboxPage() {
  return (
    <div>
      <div className="mb-6">
        <div className="t-label text-slate-500 mb-1">Sandbox</div>
        <h1 className="text-2xl font-semibold text-slate-900">Sandbox data entry</h1>
      </div>
    </div>
  );
}
